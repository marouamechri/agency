<form method="post" action="{{ path('app_appointement_delete', {'id': appointement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appointement.id) }}">
    <button class="btn btn-outline-danger">Supprimer</button>
</form>
